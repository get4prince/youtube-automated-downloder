import React from "react"
import { Text } from "react-native";

const OfflineScreen = (props) => {
    return (
        <Text>Offline now! go check out downloaded songs!</Text>
    )
}
export default OfflineScreen;